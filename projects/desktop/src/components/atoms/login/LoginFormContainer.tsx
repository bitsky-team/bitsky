import styled from 'styled-components'

import { CenteredForm } from '../singleForm/CenteredForm'

/**
 * Reducing the form size to avoid it to be ugly
 */
export const LoginFormContainer = styled(CenteredForm)`
  width: 70%;
`
